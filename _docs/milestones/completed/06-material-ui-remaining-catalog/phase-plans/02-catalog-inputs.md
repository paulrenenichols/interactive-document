# Phase plan: Catalog — inputs (material-ui-remaining-catalog)

Step-by-step execution plan. Branch from main after phase 01 is merged. Commit/push at logical points; final commit on user approval.

---

## 1. Create and check out branch

- Create and check out branch: `material-ui-remaining-catalog/02-catalog-inputs`.
- Branch from main after phase 01 is merged.

---

## 2. Input components

- Implement **Checkbox**, **Switch**, **Radio**, **Slider**, **Select**, **NumberField**, **ButtonGroup**, **ToggleButton**, **Rating**, **Autocomplete**, **FAB** per exploration scope. Each: theme tokens, `.dark`, story. Export and update library README. Use Popover/TextField/Menu where specified.
- **Checkpoint:** Commit and push at logical subgroup (e.g. after form controls, then after Autocomplete/FAB).

---

## 3. Progress doc and final step

- Add or update `_docs/progress/06-material-ui-remaining-catalog/02-catalog-inputs.md`. Update project-root README if needed.
- On user approval: final commit and push (e.g. "chore(material-ui-remaining-catalog): complete 02-catalog-inputs phase").
