# Progress: miscellaneous / docker-compose-migrations

Summary of work done on branch `docker-compose-migrations` (from commits).

## Deliverables

- **Migrations at startup:** DB migrations run on API container startup.
- **Dedicated migrations service:** Migrations service with `schema_migrations` tracking.

## Key commits

- Run DB migrations on API container startup
- Dev: dedicated migrations service with schema_migrations tracking

## Note

This branch is not tied to a milestone/phase; it is tracked under `_docs/progress/miscellaneous/`.
