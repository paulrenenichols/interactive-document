/** Max plot groups (indexed layers) or value-series rows (category_values) per chart. */
export const MAX_PLOT_GROUPS = 5;

/** Max length for global chart asset display names (binding panel + ledger). */
export const CHART_NAME_MAX_LENGTH = 64;

/** Recharts binding-panel preview: max points per series; longer series are uniformly downsampled. */
export const CHART_PREVIEW_MAX_SERIES_LENGTH = 500;

/** Max data point labels rendered chart-wide in preview/design (non-empty label strings after trim). */
export const CHART_PREVIEW_MAX_POINT_LABELS = 50;
