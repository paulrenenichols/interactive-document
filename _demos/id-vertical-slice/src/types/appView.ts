/** Root workspace mode (header hamburger). */
export type AppViewMode = "loose" | "authoring";

/** Sub-context inside Authoring view (segmented control). */
export type AuthoringContext = "dataModel" | "slideDeck";
