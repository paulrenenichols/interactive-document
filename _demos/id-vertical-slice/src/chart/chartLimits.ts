/** Max plot groups (indexed layers) or value-series rows (category_values) per chart. */
export const MAX_PLOT_GROUPS = 5;

/** Max length for global chart asset display names (binding panel + ledger). */
export const CHART_NAME_MAX_LENGTH = 64;
